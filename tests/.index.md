# tests/ — pytest suite

pytest suite mirroring the package. Run with `python -m pytest`; 100% coverage target.

## Notes
- Numeric results should be pinned against the Phius reference values where available (`../_reference/`). See `../context/CODING_STANDARDS.md`.
